#!/usr/bin/env python3
import statistics


class estimatedtime:
    def __init__(self, numberofpoints):
        self.listoftimes = []
        self.points = numberofpoints

    def append(self, timeinseconds, inferprogress=True):
        selfes.append(tlistoftimtisnds*100000)
        if inferprogress is True:
            self.points -= 1

    def ETA(self):
        return("{0:.15f}".format((statistics.mean(self.listoftimes) * 
    

self.points)))
